# DSN-0019 · Authentication and access

## DSN-0019 — Authentication and access in progress
Previous surface DSN-0018 is complete, merged and pushed; coverage 15/17. Main was clean, steering empty, no stop file, and only this run's recent surface commits. Persona frame: Anika Sharma arrives to contribute an idea; Leah Feldman to review workspace work; Devika Nair and Tobias Berg to support client operations. Before any of those jobs, each must enter the correct workspace with an existing sign-in method. Object: account/workspace entry, an invitation or a password-recovery request. Primary action: sign in with the chosen supported method, or complete the specific invitation/recovery step. Consequence: enter the authorized workspace or receive the explicit next step. Desired signal: more eligible and invited users activate successfully; guardrails are unchanged authentication/session/domain rules, no account-existence claims, preserved input and clear recovery.
Roleplay: first see Pulse/Photon Legal and the entry purpose; choose the familiar work-account method or email fallback. Read method, necessary input and one consequence, not a wall of provider choices and boilerplate. Remember no hidden instructions or lost email across recoverable errors. Skip decorative brand panels, cards and unrelated product marketing. Anxiety is choosing the wrong account, an unavailable provider, a rejected domain or an expired link/session; explain the actual next step once. Required states: default, loading, invalid credentials, unknown domain, expired/successful invitation, SSO unavailable/failure, reset requested/complete and session expired; retain every existing route and provider. This surface changes screen presentation and error/recovery display only; adapter, credential/session mechanics, provider handoffs, domain/SSO authorization and analytics remain intact. Three directions are next, before UI code.

## Directions
| Direction | Composition | Read / remember / act cost | Decision |
|---|---|---|---|
| A — Method-led entry | One narrow entry column. Google leads in existing provider order, Microsoft is secondary, workspace SSO is a quiet link, and email/password opens on demand. Recovery and signup stay below. | Read purpose and familiar provider; no extra selection/confirmation. Email users make one explicit disclosure, then see only their form and primary submit. No information to remember across columns. | Selected after all three 1280×720 renders were opened and inspected. Lowest simultaneous reading cost; no claim that a provider is authorized or preferred for an individual. |
| B — Parallel entry lanes | Work providers and the visible email/password form occupy equal columns. | All inputs and methods are visible immediately, but four routes compete across the page. An occasional Inventor must scan both lanes; zoom stacks two sections. | Rejected: heavier first glance and weaker brief hierarchy. |
| C — Method selector | A compact radio-like list selects Google, Microsoft, SSO or email, followed by Continue. | One dominant Continue, but users select a method and then confirm it before an existing handoff. The label loses the provider consequence unless repeated. | Rejected: extra decision/confirmation with no product benefit. |

Low-fi sources and renders: `directions/a.html`, `a.png`; `b.html`, `b.png`; `c.html`, `c.png`. All three renders were opened; A has the least competing form content and is being built. This is a presentation composition; supported authentication protocols and authorization rules are unchanged.

DSN-0019 presentation checkpoint: three low-fi directions inspected; method-led entry selected and built in place. Twenty V0 stories, initial required gates 7/7 and 48 stable captures pass. Full-app login recovery and password request/reset/return pass. Visual inspection found the inherited body minimum width at 200% zoom; the auth composition now joins the existing scoped reflow rule, with refreshed captures pending.

Needs the founder — signup exception: full-app probes confirm both eligible and unknown-domain emails receive HTTP 400 because the existing email-only signup maps to a password-required endpoint. Completion also maps to password reset and cannot create the new user. The exact proposed adapter/mock correction and boundary tests are recorded in changes/DSN-0019/signup-correction.md. RUN-GOALS.md excludes adapter/auth changes, and the prior client-view exception does not cover signup. No adapter or auth-handler changes have been made. Continue independent presentation validation while authorization is pending; coverage remains 15/17.

DSN-0019 recovery checkpoint: full story suite passes 393/393, both builds pass and 40 accessibility/overflow contexts are clean. Inspecting zoom captures exposed off-screen submission errors; errors now appear by submit and scroll into view, expired invitations offer a usable sign-in recovery, and the zoom header/spacing is more compact. Twenty-three scoped stories now pass, including incomplete-link recovery and reset failures. The existing missing-token redirect retains its /login destination and carries a presentation-only recovery reason. No adapter, auth handler, session or provider mechanics changed. Final capture refresh and independent evaluation remain in progress. Signup authorization is pending.

## Rendered roleplay and cognitive load
The public entry is shared by Inventor Anika, Workspace Admin Leah, Case Owner Devika and Photon Admin Tobias. No authenticated client scope or persona navigation is disclosed before entry. On the default 1280×720 and 1440×900 renders:
1. First glance: Photon Legal/Pulse identity, then Sign in to Pulse and work-account purpose. The sign-in task leads.
2. One thing: enter with the familiar work method. Continue with Google leads in the existing provider order; Microsoft is secondary, SSO and email are subordinate choices. The selected email or SSO step has its own single primary action.
3. Read: purpose, recognizable provider and action label. Skip signup/recovery unless needed. Email fields appear only after choosing that method; decorative panels and boilerplate were removed.
4. Remember: no numbers, status or client name across panels. Email and password remain in the form through errors and a round trip through method selection. Verified invitation shows the workspace and email together.
5. Never need: authenticated metrics, client queues, patent vocabulary, rankings, due dates, Actions, navigation badges or marketing panels. None appears here.
6. Anxiety: wrong account, rejected domain, unavailable SSO, expired invitation/session or incomplete password link. Persistent plain messages name the next step; recovery controls remain available. Reset-request copy makes no account-existence claim.
The observed composition passes the presentation hard rules: one primary action, no decorative card wrappers, no horizontal scroll, no duplicate status/title, and clear object/consequence labels. Full surface completion remains blocked by the separately documented signup contract defect; this cognitive-load judgment does not waive functional completion.

## What moved and stayed
Moved: public entry into a shared AuthLayout composition using existing Button/Input primitives and token values; provider hierarchy and email disclosure; persistent errors next to submit; compact zoom reflow; invitation result, unavailable-link recovery and reset-request/success states. The existing AuthField now links errors and reserves their space, with visible keyboard focus on password visibility controls. SSO unavailable replaces the ineffective primary continuation with Choose another method, while editing the email permits another attempt.
Stayed: all routes, data hooks, API calls, authentication/session/cookie behavior, Google/Microsoft/SSO handoffs and domain guard, analytics events, password rules and timed successful reset return. The two Workspace Admin reference screens are unchanged. AuthLayout is a surface composition, not a new generic UI primitive; no dependency or token-source changes.

## Mock and legacy disposition
New deterministic scenarios v0/auth/entry, expired-invitation, access-denied and long-workspace reuse the existing Northwind seed, invitation states and suspended-user state. No endpoint, role or status was added; no proposed-route declaration is needed for these presentation fixtures. The unknown-domain story deliberately injects the expected error response and is not evidence of working domain-gated signup. The full-app 400 reproduction and required correction are in signup-correction.md.
Retired the legacy Login and Auth story files and their twelve visual baseline images, including historical actual captures, and corresponding accessibility fingerprints. The login smoke journeys now use the V0 scenario and email disclosure; unrelated persona journeys are preserved.

## Evidence and remaining work
Authentication.stories.tsx contains all eleven intended IDs plus email form/validation/retention, signup form, invitation entry, SSO return, reset/forgot forms, access denied, incomplete password link, reset/reset-request errors and a long verified workspace name. Every story selects a v0/ scenario. Every story has 1280×720 and 1440×900 captures; default additionally has 1366×768, 1920×1080 and 640×360@2. Selected error/forms and long workspace also cover zoom. The shots directory is the manifest of individual captures.
24 current story interactions pass (stories-refined.log); typecheck and isolated Storybook build pass. Earlier full-suite 393/393, required gate subset 7/7, both builds and 55 stable repository captures are recorded. Final broad run, refined screenshot/accessibility refresh and independent evaluation are in progress, so these earlier checks do not claim final-branch completeness.
Full-app login correction, password request/resend with retained input, actual reset success and timed sign-in return were verified. Supplemental zoom probes verify password-eye keyboard focus and visible post-submit error. Missing-token recovery and SSO callback failure were verified. External provider hosts were blocked; no real provider login was attempted.

## Scorecard, provisional pending final review
Product fit 4, hierarchy 4, usability 4, trust 4, craft 4, accessibility 4 for the rendered presentation; business 3 because eligible email signup is currently blocked by the pre-existing contract mismatch. This is a documented incomplete outcome, not an accepted shipping exception. Intended metric: more eligible/invited users activate; guardrails: unchanged authorization, no account disclosure promises, preserved input and explicit recovery. No conversion improvement is claimed from screenshot or test evidence. Independent evaluation and final gate results will be recorded before landing.

Refined review evidence: 24/24 interactions, 48/48 accessibility/overflow contexts, and seven twice-rendered revised/new captures pass. All 58 current shots were opened and inspected, including the long workspace at zoom. Broad gates are still running on their earlier static build; independent evaluation is being requested with signup incompleteness explicit.


## DSN-0019 recovery review checkpoint — 2026-09-05T15:56:38.810732+00:00
Independent round 1 returned NEEDS_WORK: signup cannot activate eligible users, expired-reset recovery retained an unusable primary submit, and the resend label obscured its destination. The two presentation findings are corrected: terminal invalid/expired reset response shows a compact recovery view with Request a new reset link primary; confirmation now says Review email address or resend. Credential/session/API mechanics are unchanged. All five updated captures were opened individually, including reset error at 200% zoom; the explanation and primary recovery fit together without scrolling. The revised roleplay removes the choice between a dead submit and a secondary recovery. Other provisional scores remain incomplete because signup is blocked.
Current evidence: 24/24 authentication interactions, required serial gates 7/7, role lint, application and Storybook builds pass. Five captures pass twice-render stability and no egress; four focused accessibility/overflow contexts have no findings. Full-app login correction, reset request/resend with retained email, reset completion and timed return pass. Evidence is in changes/DSN-0019/recovery-evidence/. Prior broad run completed 16/23; failures cover credentials/inert hosts, screenshots, accessibility, crawl, layout, visible IDs and structure. Existing unrelated actual images remain preserved, not accepted as baselines.
Needs the founder — blocked after two fresh full-app rechecks: both eligible and unknown-domain email signup still return HTTP 400, Email and password are required. The reviewed correction remains changes/DSN-0019/signup-correction.md. No new authorization is present in STEER.md; RUN-GOALS.md and this automation explicitly prohibit adapter/auth-mechanic edits. Proceeding assumption: retain existing mechanics and leave this surface unmerged until the bounded signup exception is authorized. Independent PASS, coverage change and merge are not claimed; coverage remains 15/17. Next action is the bounded signup correction after authorization, then independent round 2 and final landing checks; DSN-0020 remains after this surface.
